initScene();

// create global coordinate system
var global_cs = new coordinate_system({x: 0, y: 0, z: 0},
                                      300,
                                      false);

animate();
